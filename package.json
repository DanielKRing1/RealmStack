{
  "name": "realm-stack",
  "version": "1.0.1",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "npm run build && npx jest",
    "build": "tsc -p tsconfig-build.json",
    "patch": "npm version patch",
    "commit": "git add . && git commit -m ",
    "push": "git push origin main",
    "pub": "npm run build && npm run commit -- \"New build\" && npm run patch && npm publish && npm run push"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@asianpersonn/dict-utils": "^1.0.3",
    "@types/jest": "^27.0.3",
    "jest": "^27.4.4",
    "ts-jest": "^27.1.1",
    "typescript": "^4.5.3"
  },
  "dependencies": {
    "dynamic-realm": "^1.0.27",
    "realm": "^10.10.1"
  }
}
