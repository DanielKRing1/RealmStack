export const MISSING_STACK_LIST_ROW_ERROR = (stackName: string) => `Missing a STACK_LIST_ROW for the stack "${stackName}"`;

export const NO_STACK_REALM_ERROR = (stackName: string) => `There is no open Realm containing the provided stack name "${stackName}"`;
